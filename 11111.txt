In the next.js program, I have written the following code that displays the content of
a single post, and it works correctly:

// pages/[post-slug].js
import { useRouter } from "next/router";
import { usePostBySlug } from "api/useApi"; // Import the custom hook
import ScrollToTopButton from "components/scroll-to-top";


const SinglePostPage = () => {
  const router = useRouter();
  const { post, isLoading, isError } = usePostBySlug(router.query["post-slug"]);

  if (isLoading) {
    return <div>Loading...</div>;
  }

  if (isError || !post) {
    return <div>Error loading the post.</div>;
  }

  return (
    <div
      className="bg-white text-gray-800 dark:bg-gray-800 dark:text-gray-200 shadow-md md:rounded-xl p-4 md:p-8 lg:m-8 md:m-4">
      <h1 className="text-center md:text-2xl text-xl font-bold p-4 lg:p-10">
        {post.title.rendered}
      </h1>
      <div
        className="prose"
        dangerouslySetInnerHTML={{ __html: post.content.rendered }}
      />
      <ScrollToTopButton />
    </div>
  );
};
export default SinglePostPage;


I have also written the following code for the 404 error:

// pages/404.js
export default function Custom404() {
  return (
    <div className="container st404">
      <h1 style="font-size: 100px">404</h1>
      <h1>page not found</h1>
    </div>
  );
}


Currently, the pages/[post-slug].js file shows a message when an error occurs.
Please use pages/404.js in this file to show the error.