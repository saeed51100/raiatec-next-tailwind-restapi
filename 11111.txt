I am using tailwindcss in my next.js application.
Using the following two files, I show the title of the site's posts:

// api/usePosts.js
import useSWR from "swr";

const fetcher = (url) => fetch(url).then((res) => res.json());
const API_URL = process.env.NEXT_PUBLIC_API_URL;

// Function to fetch post titles
export function usePosts() {
  const apiUrl = `${API_URL}/posts?per_page=20&_embed`;
  const { data } = useSWR(apiUrl, fetcher);
  return {
    posts: data,
  };
}

// Function to fetch a single post by its slug
export function usePostBySlug(slug) {
  const apiUrl = `${API_URL}/posts?slug=${slug}`;
  const { data } = useSWR(apiUrl, fetcher);

  return {
    post: data && data[0], // Assuming the slug is unique, so we take the first post found
  };
}

// components/grid.jsx
import Link from "next/link";
import { usePosts } from "api/useApi";

export default function Grid() {
  const { posts } = usePosts(); // Use the custom hook

  return (
    <>
      {posts?.map((post) => (
        <div key={post.id} className="max-w-lg mx-auto">
          <Link href={`/${post.slug}`}>{post.title.rendered}</Link>

          <br />
          <br />
          <br />
          <br />
        </div>
      ))}
    </>
  );
}

Please add pagination to my code so that 5 posts are shown on each page.