In the next.js program, I have written the file that shows the
titles of the posts:


// components/grid/index.js

import {getPosts} from "api/getPosts";


const HomePage = () => {
    const {posts, isLoading, isError} = getPosts();

    if (isLoading) {
        return <div>Loading...</div>;
    }

    if (isError) {
        return <div>Error loading posts.</div>;
    }

    return (
        <div>
            {posts.map((post) => (
                <div key={post.id}>
                    {post.title.rendered}
                </div>
            ))}
        </div>
    );
};

export default HomePage;

In the next.js program, I have written the following two-file that shows the titles of the posts:


In the // components/grid/index.js file, put a link around {post.title.rendered}
which will provide the desired page
