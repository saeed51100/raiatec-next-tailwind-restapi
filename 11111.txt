In the next.js program, I use the following file to apply lauoyt to the site pages:
// pages/_app.tsx
import "../src/global.css";
import Head from "next/head";
import Layout from "../components/layout";

export default function MyApp({ Component, pageProps }) {
return (
<>
<Layout>
<Component {...pageProps} />
</Layout>
<Head>
<title>raiatec.com</title>
</Head>
</>
);
}

How can I exclude 404 error from applying layout and use a separate style for it?