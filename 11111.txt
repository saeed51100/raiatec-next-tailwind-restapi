In the next.js program, I use the following file for title:
import "../src/global.css";
import Head from "next/head";
import Layout from "../components/layout";

export default function MyApp({ Component, pageProps }) {
   return (
     <>
       <Layout>
         <Component {...pageProps} />
       </Layout>
       <Head>
         <title>raiatec.com</title>
       </Head>
     </>
   );
}

I want the title of the post to be set as title for a single post.
Which files code should I change?