In the next.js program, I have written the file that shows the
titles of the posts:

// components/grid/index.js

import {getPosts} from "api/getPosts";
import Link from "next/link"; // Import the Link component from Next.js

const HomePage = () => {
    const {posts, isLoading, isError} = getPosts();

    if (isLoading) {
        return <div>Loading...</div>;
    }

    if (isError) {
        return <div>Error loading posts.</div>;
    }

    return (
        <div>
            {posts.map((post) => (
                <div key={post.id}>

                    <Link href={`/${post.slug}`}>
                        {post.title.rendered}
                    </Link>

                </div>
            ))}
        </div>
    );
};

export default HomePage;


please Write me the pages/[post-slug].js file so that clicking on the {post.title.rendered} title
 link will show the desired page.
